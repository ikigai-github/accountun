{
  "name": "accountun",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {    
    "standalone": "docker compose stop && docker compose up -d",
    "proof-testnet": "PROOF_SERVER_CMD='midnight-proof-server --network testnet' docker compose up -d proof-server",
    "compile": "bun run --filter @accountun/contract compile"
  }
}
